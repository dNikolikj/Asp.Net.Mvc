@model BurgerDetailsViewModel
<div class="text-center">
<h2 >@ViewBag.Blessing</h2>
<a  asp-action ="BurgerMenu"> Menu</a>
<a asp-controller="Home" asp-action ="Index"> Home</a>
</div>

<ul class="text-center" style="list-style: none;">
    @if (Model.Id == 1)
    {

    for (int i = 0; i < Model.ChefsLitlleMagic.Count; i++)
    {

    <li class="text-center">@Model.ChefsLitlleMagic[i]</li>

    }

    }

</ul>
<ul class="text-center" style="list-style: none;">
    @if (Model.Id == 2)
    {

    for (int i = 0; i < Model.ChefsLitlleMagic.Count; i++)
    {

    <li class="text-center">@Model.ChefsLitlleMagic[i]</li>

    }

    }

</ul>
<ul class="text-center" style="list-style: none;">
    @if (Model.Id == 3)
    {

    for (int i = 0; i < Model.ChefsLitlleMagic.Count; i++)
    {

    <li class="text-center">@Model.ChefsLitlleMagic[i]</li>

    }
}

</ul>






