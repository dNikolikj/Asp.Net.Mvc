
@model BurgerViewModel


<h2>@ViewBag.Blessing</h2>

<ul>
    @if (Model.Id == 1)
    {

        for(int i = 0; i <Model.ChefsLitlleMagic.Count; i++)
        {
            <li>@Model.ChefsLitlleMagic[i]</li>
        }

    }

</ul>
<ul>
    @if (Model.Id == 2)
    {

        for(int i = 0; i <Model.ChefsLitlleMagic.Count; i++)
        {
            <li>@Model.ChefsLitlleMagic[i]</li>
        }

    }

</ul>
<ul>
    @if (Model.Id == 3)
    {

        for(int i = 0; i <Model.ChefsLitlleMagic.Count; i++)
        {
            <li class= "display-5">@Model.ChefsLitlleMagic[i]</li>
        }

    }

</ul>

