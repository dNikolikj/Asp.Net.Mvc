@model List<OrderDetailsViewModel>


    <h1>There are @ViewData["NumberOfOrders"] orders made in our app.</h1>
    <h2>@ViewData["Title"]</h2>
    <h3>The first order was made by: @(((User)@ViewData["FirstUser"]).FirstName)</h3>
    <ul>
    @{

    for(int i = 0; i < @Model.Count; i++)
    {
              
        //<li>@Model.ElementAt(i).PizzaName and the price is @Model.ElementAt(i).Price.</li>
         <p>===========================================================</p>
        <partial name="~/Views/Shared/Partials/_OrderDetailsPartial.cshtml" for="@Model[i]"/>

       @* //////////////////////////////////////////////////////////////
        We need to send an object of type OrderDetailsViewModel to the partial view
        That is why we send each object from the list in ach iteration  as a model to the partial view
        //////////////////////////////////////////////////////////*@    
    }

    }
    </ul>