
@using Asp.Net.Class_03.Models.ViewModels;
@model List<PizzaViewModel>

    <ol>
        @for(int i = 0 ; i < @Model.Count; i++) 
        {
            <li>@Model.ElementAt(i).Name  is   @Model.ElementAt(i).PizzaSize size  pizza , worth @Model.ElementAt(i).Price denars.</li>
        }

    </ol>

